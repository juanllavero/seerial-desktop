import React from 'react'

function DialogCenterLeftPanel({children}: {children: React.ReactNode}) {
  return (
    <div className="dialog-center-left">{children}</div>
  )
}

export default DialogCenterLeftPanel